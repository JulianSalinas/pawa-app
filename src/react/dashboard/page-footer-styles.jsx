const styles = theme => ({
    a: {
        textDecoration: "none",
        backgroundColor: "transparent",
        color: theme.palette.primary.main
    },
    footer: {
        textAlign: "center",
        backgroundColor: "#FFF"
    }
});

export default styles;
